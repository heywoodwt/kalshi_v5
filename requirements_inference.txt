# Minimal dependencies for live trading inference only
# No training packages needed

# Core
python-dotenv==1.0.0
websockets==16.0
cryptography==49.0.0
requests==2.31.0

# For model inference only (CPU, much smaller than CUDA)
torch==2.12.1
stable-baselines3==2.2.1
numpy==2.4.6
gymnasium==0.29.1

# Data handling (minimal)
polars==1.42.0
